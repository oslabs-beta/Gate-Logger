export type QueryData = {
    complexity: number;
    // removed depth until functionality is added into limiter
    // depth: number;
    timestamp: number;
    tokens: number;
    success: boolean;
};
